import { LedSpeakerIntro } from '@/components/led-show';

export const metadata = { title: 'LED speaker · DIGITAL NIS FORUM', robots: { index: false, follow: false } };

export default function Page() {
  return <LedSpeakerIntro />;
}
