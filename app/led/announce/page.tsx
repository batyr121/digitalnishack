import { LedAnnouncement } from '@/components/led-show';

export const metadata = { title: 'LED announce · DIGITAL NIS FORUM', robots: { index: false, follow: false } };

export default function Page() {
  return <LedAnnouncement />;
}
