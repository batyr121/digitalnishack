import { LedShow } from '@/components/led-show';

export const metadata = {
  title: 'LED экран · DIGITAL NIS FORUM',
  robots: { index: false, follow: false },
};

export default function LedPage() {
  return <LedShow />;
}
